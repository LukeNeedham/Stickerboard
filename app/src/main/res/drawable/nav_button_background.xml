<?xml version="1.0" encoding="utf-8"?>
<!-- Circular, ripple-fed background for the sticker gallery's folder/ add-photo buttons. Stays
	 invisible at rest, lights up as a soft primary-tinted ripple on press, and keeps the solid
	 highlight for a button's active/ selected state. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
	android:color="@color/app_primary_ripple">

	<item android:id="@android:id/mask">
		<shape android:shape="oval">
			<solid android:color="@android:color/white" />
		</shape>
	</item>

	<item>
		<selector>
			<item android:state_selected="true">
				<shape android:shape="oval">
					<solid android:color="@android:color/white" />
				</shape>
			</item>
			<item>
				<shape android:shape="oval">
					<solid android:color="@android:color/transparent" />
				</shape>
			</item>
		</selector>
	</item>

</ripple>
